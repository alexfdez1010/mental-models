# Canonical production origin. Drives sitemap URLs, canonical tags and the
# absolute Open Graph image/page URLs emitted by Seo.astro. Override per
# environment (Vercel sets this for preview deploys to the deploy URL).
#
# TEMPLATE NOTE: change this to your real domain before deploying.
PUBLIC_SITE_URL=https://lessons.example.com
