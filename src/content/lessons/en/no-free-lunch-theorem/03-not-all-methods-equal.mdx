---
title: "It Does NOT Mean All Methods Are Equal"
description: The crucial misreading. Real problems aren't drawn evenly from all possible problems — they have structure. Matching a method's built-in assumptions to that structure is the whole game.
topic: "en/no-free-lunch-theorem"
order: 4
minutes: 13
updated: 2026-07-10
---

import { Callout, MCQ, FillBlank, Categorize, MatchConcepts, MindMap } from '@/components/react';

If the last lesson landed, a nagging question is now itching at you: *if every
method ties, why does anything work?* Weather forecasts beat coin-flips. Doctors
beat random guessing. Spell-check beats mashing keys. How can all methods be equal
when they so obviously aren't?

Here's the resolution, and it's the single most important idea in the course:
**they're only equal when averaged over *all possible* problems — and you never
face all possible problems.** The problems that actually show up in your life are a
tiny, deeply *structured* slice of the space of everything imaginable. Inside that
slice, methods differ enormously.

## The hidden assumption: "all possible problems"

Go back to the treasure hunt. The tie needed us to weight *every* hiding place
equally — the prize was just as likely to be in box 1 as box 5. But the real world
isn't like that. If you always drop your keys near the door, then "the prize is by
the door" is *common* and "the prize is in the attic" is *rare*. The hiding places
are **not** equally likely — the problem has **structure.**

<Callout variant="tip" title="The word that unlocks everything: structure">
**Structure** means the problems you actually face aren't a random grab-bag —
they follow patterns. Keys cluster near the door. Real functions are usually
smooth, not jagged. Tomorrow's weather resembles today's. Text follows grammar.
This regularity is real, and it's the thing a good method exploits.
</Callout>

Once problems have structure, the equal-weighting that forced the tie is gone —
and methods are free to differ. A method that *assumes* keys are near the door will
crush one that assumes they're uniformly scattered, **on the problems that
actually occur.** No Free Lunch is untouched (average over *everything* and they
still tie), but nobody lives in "everything."

<MCQ
  client:visible
  pretest
  question="Guess: why does 'check near the door first' beat random searching in real life, even though No Free Lunch says all searches tie?"
  options={[
    { text: 'Real lost keys aren’t uniformly scattered — they cluster, and the method matches that structure', correct: true },
    { text: 'No Free Lunch is simply wrong when tested' },
    { text: 'Random searching is against the rules' },
  ]}
  explanation="Both can be true at once. Over ALL imaginable hiding places, every search ties (No Free Lunch). But real keys have structure — they cluster near the door — and a method tuned to that structure wins on the problems you actually get."
/>

## A method's assumptions are its "prior"

Every method quietly assumes something about which problems it'll face. That
built-in assumption has a name: the method's **prior** (as in, its *prior belief*
about the world, before seeing any specific problem).

- "Check near the door first" has the prior *keys cluster near the door.*
- "Assume tomorrow's weather ≈ today's" has the prior *weather is persistent.*
- "Fit a straight line" has the prior *the relationship is roughly linear.*
- Hill-climbing has the prior *good solutions sit near other good solutions —
  the landscape is smooth.*

A method wins exactly when its prior **matches** the structure of the real
problems, and loses when it doesn't. That's the whole game. There's no such thing
as a method with *no* prior — "assume nothing" is itself a prior (the flat,
everything's-equally-likely one), and it's the one that ties with random guessing.

<FillBlank
  client:visible
  question="Pin down the mechanism."
  text="Every method carries a built-in assumption — its {{prior|penalty|price}} — about which problems it will face. It performs well exactly when that assumption {{matches|ignores|hides}} the real problem's {{structure|colour|name}}, and badly when it doesn't."
  explanation="A method's prior is its bet about the world. Matching the structure is winning the bet; mismatching it is losing. Performance is all about the fit between the two."
/>

## The misconception, said plainly

Here's the sentence to burn out of your vocabulary:

<Callout variant="warning" title="The misreading that ruins the model">
❌ *"No Free Lunch proves all methods are equally good, so it doesn't matter which
you pick."*

That's false and it's dangerous. Methods are wildly unequal **on real,
structured problems** — which is the only kind you ever meet. The theorem says
they tie *only* when averaged over a uniform grab-bag of every conceivable
problem, a situation that never occurs. Using No Free Lunch to shrug — "meh, all
tools are the same" — gets the theorem exactly backwards.
</Callout>

The right reading points the *opposite* way: because no method is universally
best, choosing the method whose assumptions fit your problem is **everything.** No
Free Lunch doesn't excuse you from choosing carefully — it's the reason careful
choosing matters at all.

<MCQ
  client:visible
  question="A colleague says: 'No Free Lunch means every forecasting method is equally good, so just use whichever.' What's the flaw?"
  options={[
    { text: 'Methods only tie over ALL possible problems; on the structured problems we actually forecast, they differ hugely', correct: true },
    { text: 'There is no flaw — forecasting methods really are all identical' },
    { text: 'No Free Lunch does not apply to forecasting at all' },
  ]}
  explanation="The tie lives in the imaginary uniform average over every conceivable problem. Real forecasting problems have structure (persistence, seasonality), and matching it separates good methods from bad. The colleague turned a precise theorem into a lazy shrug."
/>

## Two readings, side by side

The same five words — "no method is universally best" — support one wise reading
and one foolish one. Learn to tell them apart.

| | Foolish reading | Wise reading |
|---|---|---|
| Conclusion | "All methods are equal, so don't bother choosing." | "No method is best everywhere, so match the method to the problem." |
| What it ignores | Real problems have structure. | Nothing — it's built on structure. |
| What it leads to | Shrugging; using whatever's handy. | Studying the problem before picking a tool. |
| Track record | Consistently mediocre. | How every expert actually operates. |

<Categorize
  client:visible
  question="Sort each statement into the wise reading of No Free Lunch or the foolish one."
  buckets={['Wise reading', 'Foolish reading']}
  items={[
    { text: '"Let me understand this problem’s structure before I choose a tool."', bucket: 'Wise reading' },
    { text: '"All models are equal, so I’ll just use my usual one for everything."', bucket: 'Foolish reading' },
    { text: '"This method assumes smoothness; is my problem actually smooth?"', bucket: 'Wise reading' },
    { text: '"Since nothing is best, choosing carefully is pointless."', bucket: 'Foolish reading' },
    { text: '"Different problems will need different tools — I’ll keep several ready."', bucket: 'Wise reading' },
    { text: '"The theorem says effort in picking methods is wasted."', bucket: 'Foolish reading' },
  ]}
  explanation="The wise reading treats No Free Lunch as a reason to study structure and match tools to it. The foolish reading treats it as an excuse to stop thinking. Same theorem; opposite behaviour."
/>

## Why the tie and the differences both being true isn't a contradiction

It can feel paradoxical: methods tie *and* differ hugely. They don't contradict
because they're statements about **different sets of problems.**

- Over the **uniform average of all possible problems** (a set you never face):
  perfect tie.
- Over the **structured problems that actually occur** (the only set you face):
  big, exploitable differences.

Both are true at once, the way "the average human has roughly one testicle and one
ovary" and "almost no individual human does" are both true — an average over a
whole population can describe *no actual member* of it. No Free Lunch is an average
over a population of problems you'll never meet.

<MatchConcepts
  client:visible
  question="Match each term to its role in resolving the paradox."
  pairs={[
    { term: 'Structure', definition: 'The patterns real problems follow, breaking the equal-weighting' },
    { term: 'Prior', definition: 'A method’s built-in assumption about which problems it faces' },
    { term: 'Uniform average', definition: 'The imaginary grab-bag of all problems where methods tie' },
    { term: 'Matching', definition: 'Fitting a method’s prior to the real problem’s structure — the whole game' },
  ]}
  explanation="Structure breaks the tie; a prior is a method's bet about structure; the uniform average is the fiction where everything ties; matching prior to structure is where real performance comes from."
/>

## Recap

<MindMap
  client:visible
  title="Why methods still differ"
  root={{
    label: 'Not all methods are equal',
    children: [
      {
        label: 'The tie needs uniform weighting',
        children: [{ label: 'Every problem equally likely' }],
      },
      {
        label: 'Real problems have STRUCTURE',
        children: [
          { label: 'Keys cluster, functions are smooth' },
          { label: 'You never face all problems' },
        ],
      },
      {
        label: 'Each method has a PRIOR',
        children: [{ label: 'Wins when it matches the structure' }],
      },
      {
        label: 'So: match method to problem',
        children: [{ label: 'The whole game' }],
      },
    ],
  }}
/>

The theorem, properly read, isn't an excuse to stop choosing — it's the reason
choosing matters. Next we'll watch this same idea show up far from computers: in
diets, portfolios, management styles, and study habits, and why it makes *circle
of competence* and the *latticework* click into place.
