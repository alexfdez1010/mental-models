---
title: "ESS and Nash: The Extra Bar"
description: "Every evolutionarily stable strategy is a Nash equilibrium — but not every Nash equilibrium survives evolution. The extra tie-breaker that separates a stable strategy from a merely-Nash one, and the two faces of a mixed strategy: one animal flipping a coin versus a whole population split into pure types."
topic: "en/evolutionarily-stable-strategies"
order: 4
minutes: 12
updated: 2026-07-10
---

import { Callout, MCQ, Quiz, MatchConcepts, Categorize, FillBlank, Reveal, MindMap } from '@/components/react';

So far you have two tools. The **invasion test** tells you when a strategy $I$ is an **ESS**: for every mutant $J$, either $E(I,I) > E(J,I)$, or the payoffs tie against the resident ($E(I,I) = E(J,I)$) *and* $I$ wins the tie-breaker $E(I,J) > E(J,J)$. And the **Hawk–Dove** game handed you a mixed ESS at $p^* = V/C$. This lesson connects that machinery to the most famous idea in game theory — the **Nash equilibrium** — and shows exactly where the two concepts agree, where they part ways, and why the difference is not a technicality but the whole point.

The headline, in one sentence: **an ESS is a Nash equilibrium that has passed one extra exam.** Nash asks, "would a rational player want to switch?" ESS asks that too — and then asks the tougher question, "and can a lucky, drifting mutant creep in even when switching is a wash?"

<MCQ
  client:visible
  pretest
  question="Before we dig in — guess the relationship between the set of ESSs and the set of Nash equilibria in a symmetric game."
  options={[
    { text: 'Every ESS is a Nash equilibrium, but some Nash equilibria are not ESSs', correct: true },
    { text: 'Every Nash equilibrium is an ESS, but some ESSs are not Nash equilibria' },
    { text: 'They are exactly the same set — ESS is just a fancy name for Nash' },
    { text: 'They never overlap; a strategy is one or the other' },
  ]}
  explanation="ESS is a refinement of Nash: the ESS set sits strictly inside the Nash set. Passing the invasion test requires being a best reply to yourself (that's Nash) plus surviving the tie-breaker (the extra bar). So every ESS is Nash, but not conversely."
/>

## First, what a symmetric Nash equilibrium actually says

Before we can say "ESS refines Nash," pin down the Nash side in the exact language we've been using. In a **symmetric one-population game**, a strategy $I$ is a **Nash equilibrium (NE)** if it is a **best reply to itself**:

$$E(I,I) \ge E(J,I) \quad \text{for all } J.$$

Read it aloud: if everyone around you is playing $I$, no alternative strategy $J$ earns *strictly more* against that crowd than $I$ does. Nobody has a reason to unilaterally deviate. That is the entire content of Nash — a **no-regret** condition. It is about a lone rational agent staring at a fixed population and asking, "given what everyone else does, can I do better by changing my mind?" If the answer is no, you're at a Nash equilibrium.

Notice the inequality is **weak** ($\ge$, not $>$). That single choice of symbol is where all the drama lives. A **strict** Nash equilibrium uses $>$: every deviation does *strictly worse*. A **weak** Nash equilibrium allows ties: some deviation does *exactly as well*. Hold that distinction — it returns with a vengeance.

<Callout variant="info" title="Best reply to itself">
The phrase to memorize is **best reply to itself**. A symmetric NE is a strategy that, when it's the incumbent, is (at least tied for) the best thing you could be doing. If some mutant scored strictly higher against the incumbent, the incumbent wasn't a best reply — and rational players (or selection) would already be abandoning it.
</Callout>

<FillBlank
  client:visible
  text="A symmetric strategy I is a Nash equilibrium when it is a {{best reply to itself|worst reply to itself|random reply to others}}, i.e. E(I,I) is at least as large as E(J,I) for every alternative J."
/>

## ESS ⊆ Nash: every stable strategy is an equilibrium

Now the first big claim, and it's cleaner than you'd expect: **every ESS is a Nash equilibrium.** Set-wise, $\text{ESS} \subseteq \text{NE}$.

Why must this be true? Look at the two ways a strategy $I$ can pass the invasion test against a mutant $J$:

1. **Condition 1 fires:** $E(I,I) > E(J,I)$. That already gives you $E(I,I) \ge E(J,I)$ — with room to spare.
2. **Condition 1 ties, Condition 2 fires:** here $E(I,I) = E(J,I)$, which *also* gives you $E(I,I) \ge E(J,I)$ — with equality.

Either branch delivers $E(I,I) \ge E(J,I)$. Since $I$ has to beat *every* mutant $J$ to be an ESS, it satisfies $E(I,I) \ge E(J,I)$ for **all** $J$ — and that is precisely the Nash condition. So an ESS is automatically a best reply to itself.

The intuition is almost too obvious once you see it: if $I$ were *not* a best reply to itself, some $J$ would earn strictly more against the resident $I$-crowd. That $J$ would then have a fitness edge the instant it appeared and would invade immediately. A strategy that a better reply can walk straight through cannot possibly be uninvadable. **Uninvadability requires, at minimum, being a best reply to yourself.** ESS demands that floor and then some.

<Callout variant="tip" title="The logic in one line">
Not a best reply to itself → a strictly-better mutant exists → it invades → not an ESS. Contrapositive: ESS → best reply to itself → Nash. That's the whole proof of ESS ⊆ Nash.
</Callout>

<MCQ
  client:visible
  question="Why is being a best reply to itself a NECESSARY condition for a strategy I to be an ESS?"
  options={[
    { text: 'If some mutant J scored strictly more against the I-crowd, J would have a fitness edge the moment it appeared and would invade — so I could not be uninvadable', correct: true },
    { text: 'Because Nash equilibria are always mixed, and ESSs are always mixed too' },
    { text: 'Because the tie-breaker E(I,J) > E(J,J) guarantees E(I,I) is the largest possible payoff in the whole matrix' },
    { text: 'It is not necessary — a strategy can be an ESS while a mutant out-scores it against the resident' },
  ]}
  explanation="If E(J,I) > E(I,I) for some J, then J does strictly better than the resident against the resident population — it grows immediately. Uninvadability is impossible without E(I,I) >= E(J,I) for all J, which is exactly the Nash condition."
/>

## A Nash equilibrium that is NOT an ESS

Here's the payoff for all that setup: the inclusion goes only one way. **Not every Nash equilibrium is an ESS.** The ESS piles on **Condition 2**, the tie-breaker, and that extra bar knocks out equilibria that are stable to *reasoning* but not to *drift and mutation*.

Let's build the smallest possible counterexample and grind through the arithmetic. Two strategies, $A$ and $B$. Payoffs to the **row** player:

| | vs A | vs B |
|---|---|---|
| **A** | 1 | 1 |
| **B** | 1 | 2 |

Read the table the same way every time: the row is *you*, the column is your opponent, the cell is *your* payoff. So $E(A,A)=1$, $E(A,B)=1$, $E(B,A)=1$, $E(B,B)=2$.

**Step 1 — Is $A$ a Nash equilibrium?** Compare $A$ against the only alternative, $B$, when the resident is $A$:

- $E(A,A) = 1$
- $E(B,A) = 1$

They **tie**. No strategy earns strictly more than $A$ against an $A$-crowd, so $A$ is a best reply to itself. **$A$ is a (weak) Nash equilibrium.** A rational player in an all-$A$ world has no strict reason to switch — deviating to $B$ pays exactly the same 1.

**Step 2 — Is $A$ an ESS?** Condition 1 already failed (it's a tie, not a strict win), so everything rides on **Condition 2**: we need $E(A,B) > E(B,B)$.

- $E(A,B) = 1$
- $E(B,B) = 2$
- Is $1 > 2$? **No.** Condition 2 **fails.**

So **$A$ is NOT an ESS**, even though it is a genuine Nash equilibrium.

Now the evolutionary story, which is the part rationality-based Nash simply cannot see. Because $B$ does *equally well* against $A$ (both score 1), a $B$-mutant is **selectively neutral** in an $A$-world — selection neither punishes nor rewards it. So $B$ can **drift** upward in frequency by pure chance, generation after generation, with no fitness penalty stopping it. And the moment $B$ becomes common enough that $B$'s meet other $B$'s, the trap springs: $B$-vs-$B$ pays $2$, beating the $1$ that $A$-vs-$A$ pays. $B$ now has the edge, and it sweeps to fixation. **$A$ was Nash, and evolution walked right past it.**

<Callout variant="warning" title="'It's a Nash equilibrium' is not enough">
Nash asks whether a *rational deviator* would gain. Drift doesn't ask permission. A **weak** (tie) Nash equilibrium can be eroded by neutral variants that pay the same against the resident and then do better among themselves. That gap — invisible to Nash, fatal in evolution — is exactly the hole Condition 2 plugs.
</Callout>

And to complete the picture, check the other strategy, $B$, as a resident:

- $E(B,B) = 2$ versus the mutant $E(A,B) = 1$. Since $2 > 1$, Condition 1 fires: $B$ is a **strict** best reply to itself. **$B$ IS an ESS** (and a strict Nash equilibrium to boot).

<MatchConcepts
  client:visible
  question="Using the 2x2 table above (payoffs to the row player), match each claim to its correct verdict."
  pairs={[
    { term: 'A is a best reply to itself: E(A,A)=1 ties E(B,A)=1', definition: 'True — A is a (weak) Nash equilibrium' },
    { term: 'A passes the tie-breaker: E(A,B) > E(B,B)', definition: 'False — 1 > 2 is false, so A is NOT an ESS' },
    { term: 'B is a strict best reply to itself: E(B,B)=2 > E(A,B)=1', definition: 'True — B is a strict Nash equilibrium and an ESS' },
    { term: 'A B-mutant can invade an A-population', definition: 'True — it drifts in neutrally, then wins B-vs-B 2 to 1' },
  ]}
  explanation="A satisfies Nash (a tie is enough for the weak inequality) but fails ESS Condition 2, so neutral drift lets B in and then B takes over. B satisfies the strict condition and is stable."
  checkLabel="Check"
  retryLabel="Try again"
  allCorrectLabel="✓ All matched"
  termsLabel="Claims"
  definitionsLabel="Verdicts"
  explanationLabel="Explanation"
/>

<MCQ
  client:visible
  allowMultiple
  question="Select EVERY statement that is true about the A/B game above. (More than one is correct.)"
  options={[
    { text: 'A is a Nash equilibrium', correct: true },
    { text: 'A is an ESS' },
    { text: 'B is an ESS', correct: true },
    { text: 'A B-mutant is selectively neutral against an all-A population', correct: true },
    { text: 'A is uninvadable because rational players never deviate from it' },
  ]}
  explanation="A is Nash (weak) but not an ESS — it fails E(A,B) > E(B,B). B is an ESS (strict best reply to itself). A B-mutant ties A against the A-crowd, so it is neutral and can drift in; 'rational players never deviate' is irrelevant because drift, not reasoning, does the invading."
/>

## Strict Nash ⇒ ESS: the shortcut, and where the ties live

We've seen a weak Nash equilibrium fail to be an ESS. The mirror-image fact is a genuinely useful shortcut: **every strict Nash equilibrium is an ESS.**

Suppose $I$ is a **strict** Nash equilibrium — meaning $E(I,I) > E(J,I)$ for *every* $J \ne I$. But look: that inequality **is** ESS Condition 1. It holds for all mutants, so $I$ passes the invasion test outright — Condition 2 never even has to be consulted. **Strict NE → Condition 1 for all $J$ → ESS.** Done.

So where do the *interesting* ESSs live — the ones that need Condition 2 at all? Exactly where strategies **tie** against the resident. If $E(I,I) = E(J,I)$ for some mutant $J$, then $I$ is not a strict best reply to itself, so the strict-NE shortcut doesn't apply, and stability has to be rescued by the tie-breaker $E(I,J) > E(J,J)$. In those cases **Condition 2 is doing all the stability work.**

And the headline example of "strategies that tie against the resident" is the **mixed ESS**. Here's the key structural fact, worth carving into stone:

<Callout variant="info" title="Why a mixed ESS is never a strict Nash equilibrium">
In a mixed equilibrium, **every pure strategy in the support earns the same payoff against the equilibrium mix.** That equality is what makes the mix an equilibrium in the first place — if one pure component paid more, you'd shift all your weight onto it and the mix would unravel. But equal payoffs means every such pure strategy *ties* the mix against the mix. A tie is the opposite of a strict inequality, so a mixed ESS can **never** be a strict Nash equilibrium. It survives purely on Condition 2.
</Callout>

Bring back **Hawk–Dove**. Its mixed ESS $p^* = V/C$ — play Hawk with probability $V/C$ — is a **non-strict, mixed Nash equilibrium**. At that mix, pure Hawk and pure Dove earn *exactly the same* expected payoff against the equilibrium population (that's why nobody is tempted to become all-Hawk or all-Dove). Because those payoffs tie, $p^*$ flunks the strict test; it qualifies as an ESS **only through Condition 2**, which checks that the resident mix does better against a rare mutant than the mutant does against itself. The tie-breaker isn't decoration in Hawk–Dove — it's the entire reason the mix is stable.

<Categorize
  client:visible
  question="Sort each equilibrium description into how it earns (or fails) ESS status."
  buckets={['Strict NE — automatically an ESS', 'Ties against resident — needs Condition 2', 'Weak NE that FAILS Condition 2 — not an ESS']}
  items={[
    { text: 'E(I,I) > E(J,I) for every mutant J', bucket: 'Strict NE — automatically an ESS' },
    { text: 'Pure B: E(B,B)=2 > E(A,B)=1 in the earlier table', bucket: 'Strict NE — automatically an ESS' },
    { text: 'Hawk–Dove mix p* = V/C, where Hawk and Dove tie against the mix', bucket: 'Ties against resident — needs Condition 2' },
    { text: 'A generic mixed ESS: every pure strategy in the support pays the same', bucket: 'Ties against resident — needs Condition 2' },
    { text: 'Strategy A in the table: E(A,A)=E(B,A)=1 but E(A,B)=1 < E(B,B)=2', bucket: 'Weak NE that FAILS Condition 2 — not an ESS' },
  ]}
  explanation="Strict best replies to themselves are ESSs for free (Condition 1). Mixed equilibria always tie in the support, so they lean entirely on Condition 2. Strategy A ties against the resident but then loses the tie-breaker, so drift lets a neutral mutant erode it."
  checkLabel="Check"
  retryLabel="Try again"
  allCorrectLabel="✓ All sorted"
  partialResultWord="correct"
  explanationLabel="Explanation"
/>

<MCQ
  client:visible
  question="Spaced recall — In Hawk–Dove with value V and cost C (C > V > 0), the mixed ESS is p* = V/C. Which statement about this equilibrium is correct?"
  options={[
    { text: 'It is a non-strict Nash equilibrium: Hawk and Dove tie against the mix, so it is an ESS only via Condition 2', correct: true },
    { text: 'It is a strict Nash equilibrium, so Condition 1 alone makes it an ESS' },
    { text: 'It is not a Nash equilibrium at all — mixed strategies cannot be Nash equilibria' },
    { text: 'It fails the invasion test entirely, so Hawk–Dove has no ESS' },
  ]}
  explanation="At p* = V/C the two pure strategies earn equal expected payoff against the resident mix (that equality is what makes it an equilibrium). Equal payoffs mean it is non-strict, so it can only clear the ESS bar through the tie-breaker, Condition 2. It is a real ESS, just not a strict one."
/>

## The two faces of a mixed strategy

There's one more idea in this lesson, and it's the kind of distinction that separates people who *use* ESS from people who merely quote it. When we say Hawk–Dove settles at "$p^* = V/C$," what does the population *physically* look like? There are **two completely different biological pictures** that produce the identical math — and confusing them causes real errors.

**Face 1 — the randomising individual.** Every animal is a single **mixed player**: an identical stochastic gambler who, in each contest, plays Hawk with probability $p^* = V/C$ and Dove otherwise. Everyone is the same; each individual carries a coin and flips it. Variation lives *within* each animal, across its many encounters.

**Face 2 — the stable polymorphism.** Nobody randomises at all. The population is **split into pure types**: a fraction $p^*$ are lifelong pure Hawks, the remaining $1 - p^*$ are lifelong pure Doves. Each individual is deterministic; the *population* is the mixture. Variation lives *between* animals, not within them.

Here's the twist: in the standard model these two pictures are **game-theoretically equivalent.** They generate the same population-level frequency of Hawk-plays, the same expected payoffs, the same equilibrium. The math can't tell them apart. But **biologically they are different claims about the world** — is the variation you observe generated *inside* each individual (Face 1) or *across* different individuals (Face 2)? That is an empirical question with a real answer.

<Callout variant="tip" title="Same frequencies, different biology">
Ask: "Is each animal flipping a coin, or is the population a fixed blend of coin-less specialists?" The equilibrium frequency $p^*$ is identical either way, but the mechanism — within-individual randomness versus between-individual polymorphism — is a testable difference, not a matter of taste.
</Callout>

And nature has cast its vote. **Real cases lean heavily toward polymorphism.** The side-blotched lizard has three *discrete, inherited* throat-colour morphs; the left/right scale-eating fish (you'll meet them next lesson) come in two *fixed* mouth-asymmetry types. These are pure, heritable strategies carried by different individuals — not one animal privately flipping a coin before each fight. When you see a stable mix in the wild, your default guess should usually be a polymorphism of pure types, not a population of identical gamblers.

<MCQ
  client:visible
  question="A field biologist finds that 25% of a beetle species are lifelong 'fighters' and 75% are lifelong 'sneakers', and the ratio is stable across generations, with type fixed at birth. Which interpretation of the mixed equilibrium does this match?"
  options={[
    { text: 'Face 2 — a stable polymorphism: distinct pure types coexist, and the population is the mixture', correct: true },
    { text: 'Face 1 — a randomising individual: each beetle flips a coin before each contest' },
    { text: 'Neither — a stable ratio of fixed types cannot be an evolutionary equilibrium' },
    { text: 'Both are ruled out because 25/75 is not equal to any p* = V/C' },
  ]}
  explanation="Type is fixed at birth and individuals do not randomise, so this is a between-individual polymorphism (Face 2), not a within-individual mixed strategy (Face 1). Both faces can produce the same 25/75 frequencies, but the mechanism here is clearly discrete inherited types — the pattern real biology usually shows."
/>

<Reveal client:visible>
**Quick self-check before the recap:** you claimed "this equilibrium is stable because it's a Nash equilibrium." What's the one follow-up question you must ask yourself? — Is it a **strict** best reply to itself (then you're safe — strict NE ⇒ ESS), or a **weak/tie** best reply (then a neutral variant can drift in, and you must verify Condition 2, $E(I,J) > E(J,J)$, before claiming stability)? "It's an equilibrium" is a starting point, never the finish line.
</Reveal>

<Callout variant="success" title="When to use this">
Any time someone says **"that's an equilibrium, so it's stable,"** run the check: is it a *strict* best reply to itself, or a *weak* one riding on a tie? Strict best replies are evolutionarily safe. Weak ones can be quietly eroded by neutral drift — so demand the tie-breaker before you trust them. And whenever you meet a "mixed strategy," ask whether it's one animal randomising or a population of pure types coexisting; the frequencies match, but the biology doesn't.
</Callout>

<Callout variant="info" title="One loose thread for later">
Not every game even *has* an ESS. **Rock–paper–scissors** has a perfectly good mixed Nash equilibrium (play each a third of the time) that is **not** an ESS — the evolutionary dynamics *cycle* endlessly instead of settling. We'll pull that thread in an upcoming lesson; for now, just file away that "has a Nash equilibrium" does not guarantee "has an ESS."
</Callout>

## Recap

<MindMap
  client:visible
  title="ESS and Nash at a glance"
  outlineLabel="Outline"
  root={{
    label: 'ESS refines Nash',
    children: [
      {
        label: 'ESS ⊆ Nash',
        children: [
          { label: 'Symmetric NE = best reply to itself: E(I,I) >= E(J,I) for all J' },
          { label: 'Both ESS branches imply E(I,I) >= E(J,I), so every ESS is Nash' },
          { label: 'Not a best reply to itself → a better mutant invades at once' },
        ],
      },
      {
        label: 'Nash that is NOT ESS',
        children: [
          { label: 'A/B table: A is weak NE (1 ties 1) but fails E(A,B) > E(B,B): 1 > 2 is false' },
          { label: 'Neutral B drifts in, then B-vs-B pays 2 > 1 and takes over' },
          { label: 'Weak (tie) NE can be eroded by drift — Condition 2 plugs the hole' },
        ],
      },
      {
        label: 'Strict Nash ⇒ ESS',
        children: [
          { label: 'Strict NE: E(I,I) > E(J,I) for all J ≠ I — that IS Condition 1' },
          { label: 'Interesting ESSs live where strategies TIE the resident' },
          { label: 'Mixed ESS: support strategies tie, never strict, rides on Condition 2' },
          { label: 'Hawk–Dove p* = V/C is a non-strict mixed NE, ESS via Condition 2' },
        ],
      },
      {
        label: 'Two faces of a mixed strategy',
        children: [
          { label: 'Face 1 — randomising individual: each animal flips a coin at p*' },
          { label: 'Face 2 — polymorphism: population split into pure Hawk / Dove types' },
          { label: 'Same frequencies + payoffs, different biology (within vs between)' },
          { label: 'Real cases lean polymorphism: lizard morphs, scale-eating fish' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  questions={[
    {
      question: 'Which set relationship between ESSs and symmetric Nash equilibria is correct?',
      correct: 0,
      options: [
        'ESS ⊆ Nash: every ESS is a Nash equilibrium, but not conversely',
        'Nash ⊆ ESS: every Nash equilibrium is an ESS, but not conversely',
        'ESS = Nash: the two sets are identical',
        'The two sets are always disjoint',
      ],
      explanation: 'Passing the invasion test forces E(I,I) >= E(J,I) for all J (the Nash condition), so every ESS is Nash. The A/B counterexample shows a Nash equilibrium that is not an ESS, so the inclusion is strict.',
    },
    {
      question: 'Consider payoffs to the ROW player: A vs A = 3, A vs B = 3, B vs A = 3, B vs B = 4. Classify strategy A.',
      correct: 0,
      options: [
        'Nash but NOT an ESS: E(A,A)=3 ties E(B,A)=3 (weak NE), but Condition 2 needs E(A,B) > E(B,B), i.e. 3 > 4, which is false',
        'A strict Nash equilibrium and therefore an ESS',
        'Not a Nash equilibrium at all',
        'An ESS via Condition 1 because E(A,A) = 3',
      ],
      explanation: 'A is a best reply to itself only weakly (3 ties 3), so it is a weak NE. The tie-breaker fails because E(A,B)=3 is not greater than E(B,B)=4 — so a neutral B drifts in and then wins B-vs-B. A is Nash but not an ESS. (B, with E(B,B)=4 > E(A,B)=3, is the ESS.)',
    },
    {
      question: 'Why is a strict Nash equilibrium automatically an ESS?',
      correct: 0,
      options: [
        'Strict NE means E(I,I) > E(J,I) for all J ≠ I, which is exactly ESS Condition 1, so the invasion test passes without ever needing Condition 2',
        'Because strict Nash equilibria are always mixed, and mixed strategies are always stable',
        'Because Condition 2 is only required for pure strategies, and strict NE are never pure',
        'It is not automatic — strict Nash equilibria still have to pass the tie-breaker separately',
      ],
      explanation: 'The strict inequality E(I,I) > E(J,I) for all J is literally ESS Condition 1. When it holds for every mutant, the strategy is uninvadable outright and Condition 2 is never consulted.',
    },
    {
      question: 'Why can a MIXED ESS never be a strict Nash equilibrium?',
      correct: 0,
      options: [
        'Every pure strategy in the support earns the same payoff against the equilibrium mix, so they TIE — and a tie is the opposite of the strict inequality',
        'Mixed strategies are not allowed to be Nash equilibria in the first place',
        'Because mixed ESSs always fail Condition 2 and therefore are not ESSs at all',
        'Because a mixed strategy always beats every pure strategy strictly',
      ],
      explanation: 'A mix is an equilibrium precisely because its support strategies are payoff-indifferent against it — they tie. Ties preclude a strict best reply, so a mixed ESS is inherently non-strict and leans entirely on Condition 2 (as Hawk–Dove p* = V/C does).',
    },
    {
      question: 'A stable population is 60% one type and 40% another, with type inherited and fixed at birth; no individual randomises. Which "face" of a mixed strategy is this?',
      correct: 0,
      options: [
        'Face 2 — a stable polymorphism: distinct pure types coexist and the population is the mixture',
        'Face 1 — a randomising individual: each member plays each type with fixed probability',
        'Neither, because a mixed strategy must be exactly p* = V/C',
        'Both simultaneously, since they are biologically identical',
      ],
      explanation: 'Fixed, inherited types with no within-individual randomisation is a between-individual polymorphism (Face 2). It produces the same 60/40 frequencies as an equivalent randomising-individual model, but the biology (variation between individuals, not within) is different — and matches real cases like lizard morphs and scale-eating fish.',
    },
    {
      question: 'You are told a strategy is a symmetric Nash equilibrium. What single further check tells you whether it is evolutionarily stable against neutral drift?',
      correct: 0,
      options: [
        'Whether it is a STRICT best reply to itself (safe) or only a WEAK/tie best reply (then verify Condition 2, E(I,J) > E(J,J), before trusting it)',
        'Whether the payoff matrix is symmetric — if it is, every Nash equilibrium is an ESS',
        'Whether the strategy is pure — pure Nash equilibria are always ESSs',
        'Whether V > C in the underlying Hawk–Dove parameters',
      ],
      explanation: 'Strict best replies are ESSs for free. Weak (tie) best replies are exactly where neutral variants can drift in, so you must confirm the tie-breaker E(I,J) > E(J,J) holds. "It is an equilibrium" is never, by itself, proof of evolutionary stability.',
    },
  ]}
/>
